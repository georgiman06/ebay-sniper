[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
buildArgs = { NEXT_PUBLIC_API_URL = "https://ebay-sniper-production.up.railway.app/api/v1" }

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
